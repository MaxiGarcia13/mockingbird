export function AppDescription() {
  return (
    <p className="text-sm text-muted-foreground">
      A lightweight HTTP request interceptor and mocking tool for seamless API debugging and network manipulation.
    </p>
  );
}
